pipeline {
    agent any
    
    stages {

        stage('Pull image') {
            steps {
                script {
                    sh 'docker pull asasai001/lab3-cicd:nodemain-v1.0'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    sh '''
                        docker rm -f nodemain || true
                        docker run -d -p 3000:3000 --name nodemain asasai001/lab3-cicd:nodemain-v1.0
                    '''
                    
                }
            }
        }
    }
}
